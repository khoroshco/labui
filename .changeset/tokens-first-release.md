---
'@banner-lab/tokens': minor
---

Первая версия пакета токенов: `tokens.css` (три слоя — примитивы → алиасы → тема), типизированный доступ из TS (`TOKENS`, `token()`) и `tokens.json` для генераторов. Пакет не зависит от рантайма и подключается в любом стеке.
